__default__:
    queue : "short"
    time : 10
    n : 1
    mem : 200
    name : "{rule}.{wildcards}"
    log : "logs/cluster/{rule}.{wildcards}.log"
    err : "logs/cluster/{rule}.{wildcards}.err"

compute_matrix:
    time : 3
    mem : 400

plot_figures:
    time: 60
    mem: 20000

